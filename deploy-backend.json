{
    "InstanceIds": ["i-0d55f1dfb3bb7734c"],
    "DocumentName": "AWS-RunShellScript",
    "Parameters": {
        "commands": [
            "sudo docker stop innovatech_backend 2>/dev/null || true",
            "sudo docker rm innovatech_backend 2>/dev/null || true",
            "sudo docker run -d --name innovatech_backend --restart unless-stopped -p 8080:8080 -e SPRING_DATASOURCE_URL=jdbc:postgresql://10.0.2.89:5432/alumnos -e SPRING_DATASOURCE_USERNAME=postgres -e SPRING_DATASOURCE_PASSWORD=postgres 932111883716.dkr.ecr.us-east-1.amazonaws.com/alumnos-app:latest",
            "sleep 5 && sudo docker ps | grep innovatech_backend"
        ]
    }
}
