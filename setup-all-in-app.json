{
    "InstanceIds": ["i-0d55f1dfb3bb7734c"],
    "DocumentName": "AWS-RunShellScript",
    "Parameters": {
        "commands": [
            "sudo docker stop innovatech_backend 2>/dev/null || true",
            "sudo docker rm innovatech_backend 2>/dev/null || true",
            "sudo docker stop postgres_db 2>/dev/null || true",
            "sudo docker rm postgres_db 2>/dev/null || true",
            "sudo docker run -d --name postgres_db --restart unless-stopped -e POSTGRES_DB=alumnos -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -v postgres_data:/var/lib/postgresql/data -p 5432:5432 postgres:16-alpine",
            "sleep 10",
            "sudo docker run -d --name innovatech_backend --restart unless-stopped -p 8080:8080 --link postgres_db:postgres_db -e SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_db:5432/alumnos -e SPRING_DATASOURCE_USERNAME=postgres -e SPRING_DATASOURCE_PASSWORD=postgres 932111883716.dkr.ecr.us-east-1.amazonaws.com/alumnos-app:latest",
            "sleep 5 && sudo docker ps"
        ]
    }
}
